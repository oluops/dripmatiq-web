# Public — safe to ship to browser. RLS gates writes to the waitlist table.
# Reuse the same values from the mobile app's EXPO_PUBLIC_SUPABASE_*.
PUBLIC_SUPABASE_URL=https://your-project.supabase.co
PUBLIC_SUPABASE_ANON_KEY=your-anon-key
